export class Driver {
    driverid: number;
    drivername: string;

    constructor(driverid: number, drivername: string) {
        this.driverid = driverid;
        this.drivername = drivername;
    }
}
