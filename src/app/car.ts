export class Car {
    carid: number;
    carname: string;
    carpassengers: number;

    constructor(carid: number, carname: string, carpassengers: number) {
        this.carid = carid;
        this.carname = carname;
        this.carpassengers = carpassengers;
    }
}
